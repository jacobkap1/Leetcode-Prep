class Solution:
    def strStr(self, haystack: str, needle: str) -> int:

        if needle not in haystack:
            return -1
        else:
            return haystack.index(needle)

        #using built in index function which finds the index of the first occurence of needle that is in haystack
